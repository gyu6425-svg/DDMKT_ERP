# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
!**/.env.example
# 고객 에이전트 실제 설정(비번·키 포함) — 견본(agent.env.example)만 추적
agent.env
**/agent.env
# 에이전트 빌드 산출물(PyInstaller) + 배포 zip
crawler/cafe_pub/build/
crawler/cafe_pub/dist/
crawler/cafe_pub/*.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code prompt logs
logs/

# Python crawler cache
__pycache__/

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json

# 크롤러 실행 로그
crawler/*.log

# 임시 크롤/로그 스크립트
crawler/_*.py
crawler/_*.log

# 카페 고정 카드 소스 원본(대용량, 런타임 불필요 — 생성된 card-*.png만 커밋)
public/images/cafe-fixed/src/
public/images/고정 사진/
public/images/123/

# 개발 중 테스트 스크린샷/배너 (언더스코어 시작) — 커밋 안 함
public/images/_*.png

# codex 로컬 훅(머신 경로 박힘) — 커밋 제외
.codex/
# 카페 댓글 자동화 heartbeat(런타임 살아있음 신호 — 커밋 안 함)
crawler/cafe_cmt/.hb_*

# 발굴 스캔 프록시 자격증명(민감 — 절대 커밋 금지)
crawler/cafe_pub/scan_proxy.txt
crawler/cafe_kw_cache.json
