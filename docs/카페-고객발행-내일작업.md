# 카페 고객 셀프발행 — 내일 이어서 (B: 고객 ERP UI)

목표: 고객이 자기 ERP(고객 로그인) > 카페 > **"카페 자동화 발행"** 탭에서 내부 UI처럼 원고생성·발행.

## 오늘까지 (완료·검증)
- **P0 데이터층**(`docs/cafe-customer-publish-rls.sql`, ★main 실행): `publish_enabled` 승인 + 고객 큐 insert/select RLS + `my_publish_companies()`/`cafe_board_for()` + 스토리지 고객 폴더 스코프. → 검증: 더티 고객계정(ddmkt_dirty@ddmkt.com)으로 **큐 insert 201 + 위조(company=leak) 403** 통과.
- **프론트 데이터층**: `src/api/cafePublishQueue.ts` `createCustomerPublishJob` — 내 `cafe_accounts`(RLS 본인)에서 company/board 서버강제, 이미지 `<company>/<jobId>/` 경로, 미승인 차단.
- **P1 탭**: `src/components/cafe/CafeCustomerStudio.tsx` — 승인확인 → 키워드 입력 → **[원고 자동생성](CF generateCafe→generateCafeReview, layout bottom)** → 제목·본문·태그 편집 → **[발행하기]**(텍스트). `src/routes/CustomerCategoryPage.tsx` 카페 고객뷰 탭에 연결.
- **에이전트**: onedir exe 빌드·기동·발행 검증(노트북 포함). 큐→에이전트 발행 루프 실증.

## TODO
1. ✅ **사진 첨부** (2026-07-28) — `generateCafeCard` 는 누수탐지 전용이라 범용 고객 부적합 → **고객 사진 업로드**로 구현(`CafeCustomerStudio` 파일선택→1600px 축소 dataURL). `layout:'bottom'`(사진 상단·본문 하단)이라 마커 정합 불필요. 스토리지 RLS 검증: 본인 폴더 업로드 200 / 남의 폴더 400 차단.
2. ✅ **발행 현황** (2026-07-28) — `listMyCafeJobs`(RLS company 스코프) → 스튜디오 하단에 상태(대기/작성중/게시됨/완료/실패) + 게시글 링크 + 새로고침.
3. **승인 플래그 정본화(main)** — 지금 DB 는 임시 `active` 게이트. main 이 `cafe-customer-publish-rls.sql` 적용해 `publish_enabled`(기본 false)로 고정(active 기본 true라 승인게이트 부적합). 프론트는 이미 `publish_enabled!==false` 우선.
4. **배포·실사용 확인** — main 병합·Cloudflare 배포 → 더티 고객계정 웹 로그인 → 카페 > 카페 자동화 발행 탭 → 원고생성·사진첨부·발행 → 노트북 에이전트 게시까지 브라우저 end-to-end.
5. **온보딩 고객화** — `CafeOnboardTab` 로그인 크롬 런치를 고객용으로 단순화(자기 카페 1개). 에이전트 zip 배포와 연결.
6. (선택) 원고 자동생성 품질/옵션 — 분량·형식검사, 업종별 프롬프트.

## 파일 지도
- UI: `src/components/cafe/CafeCustomerStudio.tsx` · `src/routes/CustomerCategoryPage.tsx`(CafeCustomerView)
- 데이터: `src/api/cafePublishQueue.ts`(createCustomerPublishJob) · `src/api/cafeWriter.ts`(generateCafe/Review/Card) · `src/api/cafeAccounts.ts`
- RLS: `docs/cafe-customer-publish-rls.sql`(main 실행)
- 에이전트: `crawler/cafe_pub/`(agent_main·publish_listener·publish_cafe·sb_auth) · `agent.spec`/`build_agent.ps1`/`installer.iss`

## 테스트 자산
- 고객계정: ddmkt_dirty@ddmkt.com / dirty1234 (client 7d43eb04, viewer). 발행타깃 `dirtytest`(수원 광교 횟집, club 31761846, publish_enabled=true). 라이브 `dirty` 는 잠금.
- 에이전트 테스트 zip: `crawler/cafe_pub/DDMKT-Agent-TEST.zip`(agent.env 값 채움, ddmkt1 내부계정). 노트북에서 압축풀고 1-로그인.bat→2-시작.bat.
