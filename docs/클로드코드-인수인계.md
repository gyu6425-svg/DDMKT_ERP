# Claude Code 인수인계 — sub2 누수 카페 발행

작성: 2026-07-22  
작업 폴더: `C:\Users\rlawh\sub2`  
브랜치: `sub2`

## 시작할 때 반드시 읽기

1. `docs/클로드코드-인수인계.md`
2. `docs/카페-검색노출-개선.md`
3. `docs/카페발행-멀티PC-라우팅.md`
4. `docs/cafe-multi-pc.md`
5. `git status --short`와 `git diff` 확인

기존 변경과 이미지 파일은 사용자 작업이므로 삭제·reset·checkout 하지 않는다.

## 사용자 절대 규칙

- 사용자가 명시적으로 `독립검증`을 요청한 작업만 리서처·플래너·리뷰어 3역할로 검증한다.
- 누수 글은 네이버 통합검색에서 **`지역+누수탐지` 인기글 영역이 실제로 확인된 키워드만** 발행한다.
- 서울 지역은 `마포 누수탐지`가 안 뜨면 `마포구 누수탐지`처럼 `구` 포함 형태도 확인한다.
- 인기글 없음, 검색 응답 오류, 판정 실패이면 발행하지 않는다.
- 지역을 임의로 강제 발행하지 않는다.
- 같은 카페를 다른 PC의 더티클리닉 작업과 함께 사용하므로 게시판 라우팅을 유지한다.
- 누수 PC 게시판 값은 `누수`이고 `.env`의 `CAFE_BOARDS=누수`, `CAFE_BOARD=누수`와 큐 `board=누수`를 일치시킨다.
- 기존 발행 간격은 `CAFE_MIN_GAP_MIN=55`, `CAFE_MAX_GAP_MIN=70`, 종료 시각은 `19:00`이다.
- 다음 발행부터 사용자가 작성 UI를 직접 보며 속도를 확인하고 싶어 한다. 사용자가 요청하기 전에는 발행용 크롬/리스너를 함부로 재시작하지 않는다.

## 최근 완료

- 실제 네이버 화면에서 `마포구 누수탐지` 검색 시 `인테리어·DIY 인기글` 영역을 확인했다.
- 제목: `마포구 누수탐지 배관 누수 원인·증상별 대처 핵심정리`
- 큐 ID: `4a62d26c-fa86-4149-8189-a840d615905c`
- 작성 시작: 14:25:50
- 발행 완료: 14:33:08
- 소요시간: 약 7분 18초
- 게시글: `https://cafe.naver.com/ArticleRead.nhn?clubid=31754130&articleid=59&menuid=1&boardtype=L`

## 현재 발행 환경

- Python: `C:\Users\rlawh\AppData\Local\Programs\Python\Python312\python.exe`
- 리스너: `crawler/cafe_pub/publish_listener.py`
- CDP 포트: `9223`
- 크롬 프로필: `crawler/cafe_pub/chrome_profile`
- 현재 크롬은 `--headless=new`으로 실행된다.
- 다음 글부터 UI를 보여주려면 안전하게 처리해야 한다. 같은 프로필의 headless Chrome과 visible Chrome을 동시에 실행하면 안 된다.
- 리스너/크롬 전환 전 현재 processing/posted 작업이 없는지 반드시 확인한다.

## 대기 큐

마포구 발행 전 확인 당시 아래 5건이 pending이었다. 인수 시 DB에서 다시 확인할 것.

1. 양평 누수탐지
2. 가평 누수탐지
3. 연천 누수탐지
4. 분당 누수탐지
5. 동탄 누수탐지

기존 글을 삭제하거나 다시 생성하지 말고 큐 상태부터 확인한다.

## 코드 변경 요약

`crawler/cafe_pub/cafe_auto_publish.py`

- 제목은 `지역 누수탐지`로 시작한다.
- 실사 풀 12장 중 6장 무작위 선택, 기본 배너 1장으로 총 7장이다.
- 동일 배너 앞뒤 반복은 기본 해제했다. 복원은 `CAFE_REPEAT_BANNER=1`.
- 링크는 기본 약 34%만 포함한다. `CAFE_LINK_RATE`로 조절 가능하다.
- 태그는 기본 4~7개다. `CAFE_TAG_COUNT`로 고정 가능하다.
- manifest에 실험군 `meta`를 남긴다.
- `make_and_queue(..., popular_verified=False)`가 기본이며 검증 없이 직접 호출하면 발행을 차단한다.
- 인기글 분석기는 네이버 review 응답의 이스케이프 URL을 정규화하도록 수정했다. 다만 자동 판정만 믿지 말고 실제 화면으로 확인한다.

## 검색노출 분석 결론

- 설고점의 `지역+소방업체` 1위는 낮은 경쟁도, 제목 정확일치, 출처 신뢰도, 최신성의 결합일 가능성이 높다.
- 누수탐지는 경쟁이 더 강해 같은 제목 형식만으로 1위를 보장할 수 없다.
- 발행 후 10분·1시간·6시간·24시간·3일·7일 순위를 로그아웃 상태에서 기록해야 한다.
- 실제 사진을 특정 지역 현장이라고 허위로 단정하지 않는다.
- 가짜 조회·댓글·공감 등 조작은 하지 않는다.

## Caveman

- Claude Code 2.1.216 설치 완료.
- 사용자 범위 `caveman@caveman` 설치 및 enabled 확인 완료.
- 새 PowerShell에서 `claude`를 실행하면 사용 가능하다.
- 약 65% 수치는 전체 토큰 보장이 아니라 주로 출력 토큰 평균치다. 긴 분석·코딩 작업에서 사용하고 중요한 검증은 지나치게 축약되지 않았는지 확인한다.

## 첫 작업 권장 순서

1. `git status --short` 및 변경 파일 확인
2. 리스너·발행용 Chrome 프로세스 확인
3. Supabase에서 pending/processing/posted 큐 상태 확인
4. 사용자가 다음 글 발행을 요청하면 인기글 영역을 실제 화면에서 먼저 확인
5. 다음 글은 사용자가 UI를 볼 수 있는 방식으로 실행하고, 에디터 진입·입력·등록 완료 시간을 기록

